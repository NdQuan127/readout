<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="Readout turns your sources into concise AI summaries and tags."
    />
    <meta property="og:site_name" content="Readout" />
    <meta property="og:title" content="Readout" />
    <meta
      property="og:description"
      content="Concise AI summaries and tags from the sources you follow."
    />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={url(~p"/images/og-image.svg")} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Readout" />
    <meta
      name="twitter:description"
      content="Concise AI summaries and tags from the sources you follow."
    />
    <meta name="twitter:image" content={url(~p"/images/og-image.svg")} />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title default="Readout" suffix="" phx-no-format>{assigns[:page_title]}</.live_title>
    <link rel="icon" type="image/svg+xml" href={~p"/favicon.svg"} />
    <link rel="manifest" href={~p"/site.webmanifest"} />
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <script>
      (() => {
        const setTheme = (theme) => {
          if (theme === "system") {
            localStorage.removeItem("phx:theme");
            document.documentElement.removeAttribute("data-theme");
          } else {
            localStorage.setItem("phx:theme", theme);
            document.documentElement.setAttribute("data-theme", theme);
          }
        };
        if (!document.documentElement.hasAttribute("data-theme")) {
          setTheme(localStorage.getItem("phx:theme") || "system");
        }
        window.addEventListener("storage", (e) => e.key === "phx:theme" && setTheme(e.newValue || "system"));
        
        window.addEventListener("phx:set-theme", (e) => setTheme(e.target.dataset.phxTheme));
      })();
    </script>
  </head>
  <body class="font-sans bg-m3-surface text-m3-on-surface min-h-screen antialiased">
    <ul class="relative z-10 flex w-full items-center justify-end gap-4 px-4 py-3 text-sm sm:px-6 lg:px-8 text-m3-on-surface-variant">
      <%= if @current_scope do %>
        <li>
          {@current_scope.user.email}
        </li>
        <li>
          <.link href={~p"/users/settings"} class="hover:text-m3-primary">Settings</.link>
        </li>
        <li>
          <.link href={~p"/users/log-out"} method="delete" class="hover:text-m3-primary">
            Log out
          </.link>
        </li>
      <% else %>
        <li>
          <.link href={~p"/users/log-in"} class="hover:text-m3-primary">Log in</.link>
        </li>
      <% end %>
    </ul>
    {@inner_content}
  </body>
</html>
